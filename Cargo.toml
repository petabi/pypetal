[package]
name = "pypetal"
version = "0.1.0"
authors = ["Min Kim <msk@dolbo.net>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
numpy = "0.9"
petal-decomposition = "0.3"
pyo3 = { version = "0.10", features = ["extension-module"] }
rand = "0.7"
rand_chacha = "0.2"

[lib]
name = "decomposition"
crate-type = ["cdylib"]
