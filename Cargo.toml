[package]
name = "pypetal"
version = "0.1.0"
authors = ["Min Kim <msk@dolbo.net>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
numpy = "0.13"
petal-decomposition = "0.5"
pyo3 = { version = "0.13", features = ["extension-module"] }

[lib]
name = "decomposition"
crate-type = ["cdylib"]
